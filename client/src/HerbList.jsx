import React, {Component} from 'react';


const HerbList = (props) => {
    // let herbs = props.herbs.map((herb, index) => {
    //     return <li onClick{() => props.selectHerb(herb)} key={index}>{herb.name}</li>
    // })

    return(
        <div className="HerbList">
            <h1>HERB LIST</h1>
        </div>
    )
}

export default HerbList;